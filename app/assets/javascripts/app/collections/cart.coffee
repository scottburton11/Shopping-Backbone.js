App.Collections.Cart = class Cart extends Backbone.Collection

  # initialize: ->
  #   @items = []

  # add: (id) ->
  #   @items.push id
  #   @trigger "add", @items

  # contains: (id) ->
  #   id in @items